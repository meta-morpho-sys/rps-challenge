<!DOCTYPE html>

<html>

<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <link rel="stylesheet" type="text/css" href="/new_rps_style.css">
  <title>Play</title>
</head>

<div class="container">
  <header>
    <h2>
      You chose <%= @game.player1.weapon %>!
      <%= @game.player2.name %> chose <%= @game.player2.weapon %>!
    </h2>
  </header>

  <div class="content">
    <div class="chosen_weapons">
      <img src="/<%=@game.player1.weapon%>.png">
      <img src="/<%=@game.player2.weapon%>.png">
    </div>
    <h2>
      <% if @game.result == 1 %>
        The result is ....... You win!
        <% elsif @game.result == -1 %>
        The result is ....... You lose!
        <% elsif @game.result == 0 %>
        It's a draw
      <% end %>
    </h2>
  </div>
</div>

<footer>
  <a href="choice">Play again</a>
</footer>
</html>
